import React from "react";
import { RectSkeleton } from "../styles/RectangleSkeleton";

export const RectangleSkeleton = ({ width, height }) => {
    return <RectSkeleton width={width} height={height} />;
};
